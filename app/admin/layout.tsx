import type { ReactNode } from "react";

export const metadata = { title: "Admin — Mark Campbell PhD" };

export default function AdminLayout({ children }: { children: ReactNode }) {
  return <>{children}</>;
}
